# M3.1 交互验收

2026-09-15，Phase 1–5 完成。此报告记录本轮交互证据；M3 历史 A/B/C 保留原范围。没有启动 M4。

## 验收结果

| 场景                           | CLI                                                                    | GUI                                                                                   |
| ------------------------------ | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| 首次使用、无配置及服务不可用   | menu 诊断/API 回归、TTY 提示及非 TTY 退出                              | 环境查询、固定仓库 HEAD 表单事件                                                      |
| 建计划、批准不启动、Human 完成 | 25 步真实 PTY completed/PASS                                           | 真实组件表单/按钮事件 → API completed/PASS                                            |
| 三阶段 auto_until              | 菜单/API 与真实 PTY，到界后第三阶段零 Attempt，重启保持，显式继续完成  | PlanControls/HumanSubmission 事件 → API，同样边界及重启断言通过                       |
| 失败、取消及重启恢复           | 菜单/API，另有真实 PTY 取消/重启/停止核对/新尝试 PASS                  | CodeDetails 按钮/勾选事件 → API；取消后重启、停止核对与新尝试 PASS                    |
| 漂移、旧版本                   | 接受快照后修改文件拒绝；原子 CLI stale revision 回归；菜单审阅版本校验 | 接受快照后修改文件拒绝，重新核对重置接受；固定 revision/controlVersion 与过期禁用回归 |
| 真实 Codex                     | menu 问答创建、审阅、启动 → API → 真实 Codex → commands.v1 PASS        | ManualPlanCreate/PlanControls 实际事件 → API → 真实 Codex → commands.v1 PASS          |

GUI 驱动使用 React hook/query 测试替身来运行实际组件事件，未运行浏览器，不能证明视觉效果或浏览器人工体验。恢复故障注入使用 Codex 协议替身；真实 Codex 是另行执行的两个简单代码任务，没有把故障替身计作真实模型运行。CLI 真实模型入口使用 menu 的问答接口；PTY 按键另行覆盖 Human、恢复与边界。

## 本轮真实代码证据

在两个专用临时 Git 仓库中，从错误的 `sum=()=>0` 开始，人工表单或问答定义只修改 `sum.mjs`，独立 Node 断言检查 `sum(2,3)===5` 和 `sum(-2,1)===-1`。两端均有实际 diff、文件内容、冻结 Package、Attempt、会话以及 `ALL_COMMANDS_PASSED`，不以 Agent 正常退出代替完成。

- [GUI 完整结果](evidence/m3.1/gui.json)：Attempt `e5c834d8-8dec-4f5a-8d4b-c0bddbe3ed4c`，completed/PASS，约 67.7 秒。
- [CLI 完整结果](evidence/m3.1/cli.json)：Attempt `35c5df52-d5b3-4654-9880-5b835059a480`，completed/PASS，约 68.4 秒。
- [SHA-256 清单](evidence/m3.1/manifest.json) 固定导出字节，结果内包含证据内容及原证据 hash，不仅引用临时文件。
- [PTY 恢复与边界结果](evidence/m3.1/pty-recovery.json)、[按键记录](evidence/m3.1/pty-recovery.txt)、[Human 按键记录](evidence/m3.1/pty-human.txt)。

环境为 macOS、Node.js 25.8.2、pnpm 10.27.0，Runtime adapter 标识保存在结果中。模型按本机既有配置执行，结果中的 model 为 null 时记 unavailable；不估算模型版本或费用。未改全局配置、未提交/推送/部署。未测试 Node.js 24。

## 复跑与检查

常规 `pnpm check`：23 文件 / 102 项测试通过、类型检查与全量构建通过。两项真实模型测试默认 skipped，已单独显式运行并通过。`pnpm format:check`、`git diff --check` 通过。Vite 仅有第三方 Zod 注释警告。

```sh
python3 scripts/interaction/menu-pty.py
python3 scripts/interaction/recovery-pty.py
pnpm exec vitest run apps/web/src/interaction.test.ts
# 以下显式调用真实模型，需本机已配置认证，保留临时结果目录：
MERFORGE_REAL_CODEX=/opt/homebrew/bin/codex pnpm exec vitest run apps/web/src/interaction.test.ts -t 'real Codex from interaction entry'
```

HTTP/PTY/进程核对需要正常本机权限。初次沙箱 EPERM、测试配置和驱动定位错误均已修正后重跑；没有将失败结果伪记为通过。缺少认证或执行权限的未来复跑应记录具体 blocked，`--version` 探测不代表认证有效。

恢复仍受 M3 合同限制：本地可信且不守护化的进程；未知身份、工作区漂移和证据缺失保持阻塞。取消请求不证明进程停止，同会话 resume unsupported，恢复通过明确创建新 Attempt。
